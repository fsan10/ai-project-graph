# 参与贡献

感谢你帮助完善 AI 项目图谱。提交改动前，请先搜索现有 Issue，避免重复工作。

## 开发流程

1. 使用 Node.js 22.13 或更高版本安装依赖：`npm install`。
2. 使用 `npm run dev` 启动本地开发环境。
3. 为领域规则或交互行为补充自动化测试。
4. 依次运行 `npm run typecheck`、`npm run lint` 和 `npm test`。
5. 提交聚焦、可回滚的改动，并在 Pull Request 中说明验证结果。

## 设计约束

- 节点稳定标识创建后不可变。
- 智能体不能直接将节点标记为“已完成”；必须经过用户验收。
- 领域写入必须携带预期修订号，冲突时拒绝覆盖。
- 代码证据默认拒绝：来源或校验信息不完整时不得视为有效。
- 画布事件必须区分节点、连线和空白区域，并同时考虑鼠标与触控操作。
- 所有用户界面文案使用简体中文；代码标识与协议字段可保留英文。

## 提交信息

建议使用简短的动词开头，例如：`修复画布拖拽白屏`、`增加连线批量编辑`。不要把无关格式化或生成文件混入功能提交。
